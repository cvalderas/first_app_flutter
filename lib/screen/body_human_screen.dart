import 'package:flutter/material.dart';

class HumanBodyScreen extends StatelessWidget {
  const HumanBodyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('HumanBody'),
      ),
    );
  }
}
