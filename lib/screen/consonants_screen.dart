import 'package:flutter/material.dart';

class ConsonantsScreen extends StatelessWidget {
  const ConsonantsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Consonants'),
      ),
    );
  }
}
